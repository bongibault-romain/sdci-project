# Service ClusterIP for Application
apiVersion: v1
kind: Service
metadata:
  name: sdci-application-clusterip
spec:
  selector:
    app: sdci-application
  ports:
  - protocol: TCP
    name: http    
    port: 7001
    targetPort: 7001
  type: ClusterIP