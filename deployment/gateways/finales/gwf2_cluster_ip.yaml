# Service ClusterIP for Gateway i
apiVersion: v1
kind: Service
metadata:
  name: sdci-gwf2-clusterip
spec:
  selector:
    app: sdci-gwf2
  ports:
  - protocol: TCP
    name: http    
    port: 8383
    targetPort: 8383
  type: ClusterIP