# Service ClusterIP for Gateway i
apiVersion: v1
kind: Service
metadata:
  name: sdci-gateway-finale1-clusterip
spec:
  selector:
    app: sdci-gateway-finale1
  ports:
  - protocol: TCP
    name: http    
    port: 8282
    targetPort: 8282
  type: ClusterIP