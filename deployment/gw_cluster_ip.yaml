# Service ClusterIP for Gateway i
apiVersion: v1
kind: Service
metadata:
  name: sdci-gateway-clusterip
spec:
  selector:
    app: sdci-gateway
  ports:
  - protocol: TCP
    name: http    
    port: 8181
    targetPort: 8181
  type: ClusterIP