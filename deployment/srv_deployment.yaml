# Deployment Server
apiVersion: apps/v1
kind: Deployment
metadata:
  name: sdci-server
spec:
  selector:
    matchLabels:
      app: sdci-server
  replicas: 1
  template:
    metadata:
      labels:
        app: sdci-server
    spec:
      containers:
      - name: sdci-server-container
        image: sdci-server:v1
        imagePullPolicy: Never
        ports:
        - containerPort: 8080
        env:
        - name: LOCAL_IP
          value: "sdci-server-clusterip"
        - name: LOCAL_PORT
          value: "8080"
        - name: LOCAL_NAME
          value: "srv"
