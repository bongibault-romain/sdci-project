# Deployment Gateway finale
apiVersion: apps/v1
kind: Deployment
metadata:
  name: sdci-gateway-finale1
spec:
  selector:
    matchLabels:
      app: sdci-gateway-finale1
  replicas: 1
  template:
    metadata:
      labels:
        app: sdci-gateway-finale1
    spec:
      containers:
      - name: sdci-gateway-finale1-container
        image: sdci-gateway:v1
        imagePullPolicy: Never
        ports:
        - containerPort: 8282
        env:
        - name: LOCAL_IP
          value: "sdci-gateway-finale1-clusterip"
        - name: LOCAL_PORT
          value: "8282"
        - name: LOCAL_NAME  
          value: "gwf1"
        - name: REMOTE_IP
          value: "sdci-gateway-inter-clusterip"
        - name: REMOTE_PORT
          value: "8181"
        - name: REMOTE_NAME
          value: "sdci-gateway-inter"